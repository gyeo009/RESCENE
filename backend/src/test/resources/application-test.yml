spring:
  datasource:
    url: jdbc:h2:mem:trouble_lab;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  docker:
    compose:
      enabled: false
